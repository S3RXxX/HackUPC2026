# 1. Emojis in comments (LLM-style)
def calculate_total(items):
    # 🚀 Let's optimize this function 
    # Python is amazing! 🐍✨
    total = sum(items)
    # TODO: Add more awesome sauce 🎉
    return total

# 2. Stack Overflow comment left in code
def parse_json(data):
    # Found this on Stack Overflow, hope it works 🤞
    # https://stackoverflow.com/questions/12345
    import json
    return json.loads(data)

# 3. Helper function that does everything
def helper():
    # Validates input
    # Calculates prices
    # Saves to database
    # Sends emails
    # Updates cache
    # Logs everything
    # Returns None (sometimes)
    pass

# 5. Single-letter variable names
def calculate(x, y, z, a, b, c):
    d = x + y
    e = z * a
    f = b - c
    g = d * e
    h = g + f
    i = h / 2
    return i

# 6. Comparing boolean to True
def is_valid(user):
    if user.is_active == True:
        if user.has_permission == True:
            return True
    return False

# 7. Commented out print debugging
def process_data(data):
    # print("Starting process...")
    result = data * 2
    # print("Result:", result)
    # print("Ending process...")
    return result


#8. Thing1, Thing2 variable names
def process_users(users):
    thing1 = []
    thing2 = []
    thing3 = {}
    for thing4 in users:
        thing1.append(thing4.name)
        thing3[thing4.id] = thing4
    thing2 = [x for x in thing1 if x]
    return thing2


# 12. "Don't touch this" comment
def magic_function(x):
    # I have no idea why this works but don't touch it
    y = x * (x + 1) / (x + 2) - (x - 1) ** 2
    return y + 42


# 14. Global counter from 8 functions
global_counter = 0

def func1():
    global global_counter
    global_counter += 1

def func2():
    global global_counter
    global_counter += 2

def func3():
    global global_counter
    global_counter -= 1
# ... 5 more functions modifying same global


# 16. Unused imports
import os
import sys
import json
import csv
import re
import math
import random
import datetime
from collections import defaultdict
from functools import reduce

# Only using math.sqrt()
def calculate(x):
    return math.sqrt(x)